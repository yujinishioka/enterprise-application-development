@model Filme

@{
    ViewData["Title"] = "Info";
}

<h1>@Model.Titulo</h1>

<table class="table table-striped">
    <tr>
        <th>Sinopse</th>
        <th>Duração</th>
        <th>Bilheteria</th>
        <th>Data Lançamento</th>
        <th>Categoria</th>
        <th>Disponível</th>
        <th>Produtora</th>
    </tr>
    <tr>
        <td>@Model.Sinopse</td>
        <td>@Model.Duracao</td>
        <td>@Model.Bilheteria</td>
        <td>@Model.DataLancamento.ToShortDateString()</td>
        <td>@Model.Categoria</td>
        <td>@(Model.Disponivel?"Sim":"Não")</td>
        <td>@Model.Produtora?.Nome</td>
    </tr>
</table>

<h2>Atores Disponíveis</h2>

<table class="table">
    <tr>
        <th>Nome</th>
    </tr>
    @foreach (var item in ViewBag.atores)
    {
        <tr>
            <td>@item.Nome</td>
        </tr>
    }
</table>